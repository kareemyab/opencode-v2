# Strings/patterns that may keep "opencode" after locale sweep (not user-facing product name)
#
# Use with scripts/check-orgn-branding.sh — paths or regex exceptions documented here.

# Provider IDs and SDK enums (not product branding)
opencodeZen
opencodeGo
ProviderV2.ID.opencode
providerID: ProviderV2.ID.opencode
"opencode"

# Config file references (compat mode)
opencode.json
opencode.jsonc
.opencode/

# Internal storage/runtime (compat mode — legacy read paths)
opencode-theme-id
opencode-color-scheme
opencode.settings
opencode.global.dat
OPENCODE_CHANNEL
OPENCODE_

# Internal WSL job kinds (not user-visible)
probe-opencode
install-opencode

# Legacy deep link transition (remove after one release)
opencode://
opencode:deep-link
__OPENCODE__

# Package scopes (out of scope for user-visible rebrand)
@opencode-ai/

# Legacy Tauri app IDs for one-time migration (migrate.ts only)
ai.opencode.desktop

# npm package names in uninstall hints (CLI compat — revisit in CLI rename ticket)
opencode-ai
